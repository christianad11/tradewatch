import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TradeWatch Lebanon | Statistical trade anomaly triage",
  description:
    "An auditable research prototype that prioritizes unusual Lebanon mirror-trade flows for human investigation.",
  openGraph: {
    title: "TradeWatch Lebanon",
    description: "Statistical trade anomaly triage across 4,931 mirror pairs.",
    images: [{ url: "/og.png", width: 1200, height: 630 }],
  },
  twitter: {
    card: "summary_large_image",
    title: "TradeWatch Lebanon",
    description: "Statistical trade anomaly triage across 4,931 mirror pairs.",
    images: ["/og.png"],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
